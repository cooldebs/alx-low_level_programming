#include <stdio.h>
/**
 * main- Prints all single digit numbers of base 10.
 * All your code should be in the main function.
 * Return: 0
 */
int main(void)
{
	int v;

	while (v < 10)
	{
		printf("%d", v);
		v++;
	}
	printf("\n");
	return (0);
}
