#include "main.h"
/**
 * int _abs- This function computes the absolute value of an integer.
 * @m: This is the integer input.
 * Return: The absolute value of m.
 */
int _abs(int)
{
	return (m * ((m > 0) - (m < 0)));
}
