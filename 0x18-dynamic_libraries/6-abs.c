#include "main.h"

/**
 * _abs- Function that computes the absolute value of an integer.
 * @n: Integer input.
 *
 * Return: Absolute value of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
