#include "main.h"

/**
 * print_numbers- a function that prints the numbers, from 0 to 9
 *
 * Return: void
 */
void print_numbers(void)
{
	int numbers;

	for (numbers = 0; numbers = 9; number++)
	{
		_putchar((numbers % 10) + '0');
		_putchar('\n');
	}
}
